### UNsecured test page

server {
    listen 80;
    server_name default_server;

    location / {
        try_files $uri $uri/ =404;        
    }

    # error pages
    include /etc/nginx/errorpages.conf;
}
